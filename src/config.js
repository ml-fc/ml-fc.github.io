export const CONFIG = {
   API_BASE: "https://mlfc-api.manorlakes-football.workers.dev"
};

// Bump this on each deploy so installed PWAs can reliably detect a new version.
// Used for footer display and as a cache-busting query param for service-worker registration.
export const BUILD_ID = "2026-01-27.8";
